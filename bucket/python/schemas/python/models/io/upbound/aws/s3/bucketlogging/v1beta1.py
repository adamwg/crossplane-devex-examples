# generated by datamodel-codegen:
#   filename:  workdir/s3_aws_upbound_io_v1beta1_bucketlogging.yaml

from __future__ import annotations

from datetime import datetime
from typing import Any, Dict, List, Literal, Optional

from pydantic import BaseModel, Field

from .....k8s.apimachinery.pkg.apis.meta import v1


class Policy(BaseModel):
    resolution: Optional[Literal['Required', 'Optional']] = 'Required'
    """
    Resolution specifies whether resolution of this reference is required.
    The default is 'Required', which means the reconcile will fail if the
    reference cannot be resolved. 'Optional' means this reference will be
    a no-op if it cannot be resolved.
    """
    resolve: Optional[Literal['Always', 'IfNotPresent']] = None
    """
    Resolve specifies when this reference should be resolved. The default
    is 'IfNotPresent', which will attempt to resolve the reference only when
    the corresponding field is not present. Use 'Always' to resolve the
    reference on every reconcile.
    """


class BucketRef(BaseModel):
    name: str
    """
    Name of the referenced object.
    """
    policy: Optional[Policy] = None
    """
    Policies for referencing.
    """


class BucketSelector(BaseModel):
    matchControllerRef: Optional[bool] = None
    """
    MatchControllerRef ensures an object with the same controller reference
    as the selecting object is selected.
    """
    matchLabels: Optional[Dict[str, str]] = None
    """
    MatchLabels ensures an object with matching labels is selected.
    """
    policy: Optional[Policy] = None
    """
    Policies for selection.
    """


class TargetBucketRef(BaseModel):
    name: str
    """
    Name of the referenced object.
    """
    policy: Optional[Policy] = None
    """
    Policies for referencing.
    """


class TargetBucketSelector(BaseModel):
    matchControllerRef: Optional[bool] = None
    """
    MatchControllerRef ensures an object with the same controller reference
    as the selecting object is selected.
    """
    matchLabels: Optional[Dict[str, str]] = None
    """
    MatchLabels ensures an object with matching labels is selected.
    """
    policy: Optional[Policy] = None
    """
    Policies for selection.
    """


class GranteeItem(BaseModel):
    emailAddress: Optional[str] = None
    """
    Email address of the grantee. See Regions and Endpoints for supported AWS regions where this argument can be specified.
    """
    id: Optional[str] = None
    """
    Canonical user ID of the grantee.
    """
    type: Optional[str] = None
    """
    Type of grantee. Valid values: CanonicalUser, AmazonCustomerByEmail, Group.
    """
    uri: Optional[str] = None
    """
    URI of the grantee group.
    """


class TargetGrantItem(BaseModel):
    grantee: Optional[List[GranteeItem]] = None
    """
    Configuration block for the person being granted permissions. See below.
    """
    permission: Optional[str] = None
    """
    Logging permissions assigned to the grantee for the bucket. Valid values: FULL_CONTROL, READ, WRITE.
    """


class PartitionedPrefixItem(BaseModel):
    partitionDateSource: Optional[str] = None
    """
    Specifies the partition date source for the partitioned prefix. Valid values: EventTime, DeliveryTime.
    """


class TargetObjectKeyFormatItem(BaseModel):
    partitionedPrefix: Optional[List[PartitionedPrefixItem]] = None
    """
    Partitioned S3 key for log objects, in the form [target_prefix][SourceAccountId]/[SourceRegion]/[SourceBucket]/[YYYY]/[MM]/[DD]/[YYYY]-[MM]-[DD]-[hh]-[mm]-[ss]-[UniqueString]. Conflicts with simple_prefix. See below.
    """
    simplePrefix: Optional[List[Dict[str, Any]]] = None
    """
    Use the simple format for S3 keys for log objects, in the form [target_prefix][YYYY]-[MM]-[DD]-[hh]-[mm]-[ss]-[UniqueString]. To use, set simple_prefix {}. Conflicts with partitioned_prefix.
    """


class ForProvider(BaseModel):
    bucket: Optional[str] = None
    """
    Name of the bucket.
    """
    bucketRef: Optional[BucketRef] = None
    """
    Reference to a Bucket in s3 to populate bucket.
    """
    bucketSelector: Optional[BucketSelector] = None
    """
    Selector for a Bucket in s3 to populate bucket.
    """
    expectedBucketOwner: Optional[str] = None
    """
    Account ID of the expected bucket owner.
    """
    region: str
    """
    Region where this resource will be managed. Defaults to the Region set in the provider configuration.
    Region is the region you'd like your resource to be created in.
    """
    targetBucket: Optional[str] = None
    """
    Name of the bucket where you want Amazon S3 to store server access logs.
    """
    targetBucketRef: Optional[TargetBucketRef] = None
    """
    Reference to a Bucket in s3 to populate targetBucket.
    """
    targetBucketSelector: Optional[TargetBucketSelector] = None
    """
    Selector for a Bucket in s3 to populate targetBucket.
    """
    targetGrant: Optional[List[TargetGrantItem]] = None
    """
    Set of configuration blocks with information for granting permissions. See below.
    """
    targetObjectKeyFormat: Optional[List[TargetObjectKeyFormatItem]] = None
    """
    Amazon S3 key format for log objects. See below.
    """
    targetPrefix: Optional[str] = None
    """
    Prefix for all log object keys.
    """


class InitProvider(BaseModel):
    bucket: Optional[str] = None
    """
    Name of the bucket.
    """
    bucketRef: Optional[BucketRef] = None
    """
    Reference to a Bucket in s3 to populate bucket.
    """
    bucketSelector: Optional[BucketSelector] = None
    """
    Selector for a Bucket in s3 to populate bucket.
    """
    expectedBucketOwner: Optional[str] = None
    """
    Account ID of the expected bucket owner.
    """
    targetBucket: Optional[str] = None
    """
    Name of the bucket where you want Amazon S3 to store server access logs.
    """
    targetBucketRef: Optional[TargetBucketRef] = None
    """
    Reference to a Bucket in s3 to populate targetBucket.
    """
    targetBucketSelector: Optional[TargetBucketSelector] = None
    """
    Selector for a Bucket in s3 to populate targetBucket.
    """
    targetGrant: Optional[List[TargetGrantItem]] = None
    """
    Set of configuration blocks with information for granting permissions. See below.
    """
    targetObjectKeyFormat: Optional[List[TargetObjectKeyFormatItem]] = None
    """
    Amazon S3 key format for log objects. See below.
    """
    targetPrefix: Optional[str] = None
    """
    Prefix for all log object keys.
    """


class ProviderConfigRef(BaseModel):
    name: str
    """
    Name of the referenced object.
    """
    policy: Optional[Policy] = None
    """
    Policies for referencing.
    """


class WriteConnectionSecretToRef(BaseModel):
    name: str
    """
    Name of the secret.
    """
    namespace: str
    """
    Namespace of the secret.
    """


class Spec(BaseModel):
    deletionPolicy: Optional[Literal['Orphan', 'Delete']] = 'Delete'
    """
    DeletionPolicy specifies what will happen to the underlying external
    when this managed resource is deleted - either "Delete" or "Orphan" the
    external resource.
    This field is planned to be deprecated in favor of the ManagementPolicies
    field in a future release. Currently, both could be set independently and
    non-default values would be honored if the feature flag is enabled.
    See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
    """
    forProvider: ForProvider
    initProvider: Optional[InitProvider] = None
    """
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.
    """
    managementPolicies: Optional[
        List[Literal['Observe', 'Create', 'Update', 'Delete', 'LateInitialize', '*']]
    ] = ['*']
    """
    THIS IS A BETA FIELD. It is on by default but can be opted out
    through a Crossplane feature flag.
    ManagementPolicies specify the array of actions Crossplane is allowed to
    take on the managed and external resources.
    This field is planned to replace the DeletionPolicy field in a future
    release. Currently, both could be set independently and non-default
    values would be honored if the feature flag is enabled. If both are
    custom, the DeletionPolicy field will be ignored.
    See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
    and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    """
    providerConfigRef: Optional[ProviderConfigRef] = Field(
        default_factory=lambda: ProviderConfigRef.model_validate({'name': 'default'})
    )
    """
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.
    """
    writeConnectionSecretToRef: Optional[WriteConnectionSecretToRef] = None
    """
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.
    """


class GranteeItemModel(BaseModel):
    displayName: Optional[str] = None
    emailAddress: Optional[str] = None
    """
    Email address of the grantee. See Regions and Endpoints for supported AWS regions where this argument can be specified.
    """
    id: Optional[str] = None
    """
    Canonical user ID of the grantee.
    """
    type: Optional[str] = None
    """
    Type of grantee. Valid values: CanonicalUser, AmazonCustomerByEmail, Group.
    """
    uri: Optional[str] = None
    """
    URI of the grantee group.
    """


class AtProvider(BaseModel):
    bucket: Optional[str] = None
    """
    Name of the bucket.
    """
    expectedBucketOwner: Optional[str] = None
    """
    Account ID of the expected bucket owner.
    """
    id: Optional[str] = None
    """
    The bucket or bucket and expected_bucket_owner separated by a comma (,) if the latter is provided.
    """
    region: Optional[str] = None
    """
    Region where this resource will be managed. Defaults to the Region set in the provider configuration.
    Region is the region you'd like your resource to be created in.
    """
    targetBucket: Optional[str] = None
    """
    Name of the bucket where you want Amazon S3 to store server access logs.
    """
    targetGrant: Optional[List[TargetGrantItem]] = None
    """
    Set of configuration blocks with information for granting permissions. See below.
    """
    targetObjectKeyFormat: Optional[List[TargetObjectKeyFormatItem]] = None
    """
    Amazon S3 key format for log objects. See below.
    """
    targetPrefix: Optional[str] = None
    """
    Prefix for all log object keys.
    """


class Condition(BaseModel):
    lastTransitionTime: datetime
    """
    LastTransitionTime is the last time this condition transitioned from one
    status to another.
    """
    message: Optional[str] = None
    """
    A Message containing details about this condition's last transition from
    one status to another, if any.
    """
    observedGeneration: Optional[int] = None
    """
    ObservedGeneration represents the .metadata.generation that the condition was set based upon.
    For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
    with respect to the current state of the instance.
    """
    reason: str
    """
    A Reason for this condition's last transition from one status to another.
    """
    status: str
    """
    Status of this condition; is it currently True, False, or Unknown?
    """
    type: str
    """
    Type of this condition. At most one of each condition type may apply to
    a resource at any point in time.
    """


class Status(BaseModel):
    atProvider: Optional[AtProvider] = None
    conditions: Optional[List[Condition]] = None
    """
    Conditions of the resource.
    """
    observedGeneration: Optional[int] = None
    """
    ObservedGeneration is the latest metadata.generation
    which resulted in either a ready state, or stalled due to error
    it can not recover from without human intervention.
    """


class BucketLogging(BaseModel):
    apiVersion: Optional[Literal['s3.aws.upbound.io/v1beta1']] = (
        's3.aws.upbound.io/v1beta1'
    )
    """
    APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    """
    kind: Optional[Literal['BucketLogging']] = 'BucketLogging'
    """
    Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    """
    metadata: Optional[v1.ObjectMeta] = None
    """
    Standard object's metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
    """
    spec: Spec
    """
    BucketLoggingSpec defines the desired state of BucketLogging
    """
    status: Optional[Status] = None
    """
    BucketLoggingStatus defines the observed state of BucketLogging.
    """


class BucketLoggingList(BaseModel):
    apiVersion: Optional[str] = None
    """
    APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    """
    items: List[BucketLogging]
    """
    List of bucketloggings. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md
    """
    kind: Optional[str] = None
    """
    Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    """
    metadata: Optional[v1.ListMeta] = None
    """
    Standard list metadata. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    """